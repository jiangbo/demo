spatial 核心未实现

  7. 没有 CollisionResult 综合结果。C++ checkCollision() 会同时返回静态碰撞、方向阻挡、hazard/
     water、动态碰撞体列表。Zig 的 spatial.isBlocked() 只返回 bool。

  8. 没有 dirty 驱动的空间索引同步。C++ 有 SpatialIndexTag、TransformDirtyTag、SpatialIndexSystem，
     移动后更新动态网格。Zig 的 /abs/path/C:/workspace/demo/farm/system/movement.zig:13 只更新位置，
     不维护动态索引。

  9. 玩家/NPC/动物之间没有动态碰撞。Zig 玩家和动物都有 Collider，但 movement 只查
     spatial.isBlocked()，不会查其他实体。所以动态实体互相阻挡、重叠规避、移动后索引同步都没实现。

  10. 地图加载没有写入静态空间层实体。C++ LevelLoader 会把每个 tile entity 注册到 StaticTileGrid 的
     layer。Zig 的 /abs/path/C:/workspace/demo/farm/map.zig:207 只生成渲染顶点和碰撞 flag，不保存可
     查询的瓦片实体。

  11. 对象层没有统一注册到空间索引。Zig 现在 collider object 只进 spatial.areas，地图 prop 只生成渲
     染实体并额外加静态矩形；没有像 C++ 那样把 rest area、rock、tree、crop、chest 等按 layer 或
     collider 加入索引。

  12. 交互系统未接空间索引。C++ 会按玩家朝向构建 probe rect，查动态碰撞体，优先 NPC > chest，再
     fallback 到静态 REST layer。Zig 的 /abs/path/C:/workspace/demo/farm/system/talk.zig:47 是遍历所
     有 NPC，按距离选最近，不看朝向、不查 probe、不支持宝箱/休息区 fallback。

  13. 农场系统未接空间索引。C++ 锄地前检查 ARABLE、SOLID、OCCUPIED、已有 crop/soil、动态 AABB 阻挡，
     并忽略待删除实体。Zig 的 /abs/path/C:/workspace/demo/farm/map/land.zig:53 只判断当前格是否已有
     land/crop。

  14. C++ 的薄墙 sweep 查询未实现。Zig 有方向阻挡即时检测，但没有 sweepVertical/sweepHorizontal、
     resolveStaticSweep()、SweepHitInfo。这块教程说细节放到 20，但 C++ 空间模块里已经有。

  15. Debug 面板未实现。Zig 只有碰撞 tile/rect 数量和简单绘制，见 /abs/path/C:/workspace/demo/farm/
     ui/debug.zig:187 与 /abs/path/C:/workspace/demo/farm/scene.zig:163。C++ 有静态/动态网格可视化、
     cell 高亮、实体 bounds、查询测试、候选/命中列表。

  16. 对应单测缺失。Zig 已有 isBlocked、land/tool/talk 的局部测试，但没有动态网格边界/去重/更新、静
     态 layer 查询、Manager 初始化/重置、broadphase/narrowphase、空间索引驱动的交互/锄地阻挡测试。
